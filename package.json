{
  "name": "template-warm-local",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "[ -f build.env ] && set -a && . ./build.env && set +a; astro dev",
    "build": "[ -f build.env ] && set -a && . ./build.env && set +a; astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "tailwindcss": "^4.3.3",
    "@tailwindcss/vite": "^4.3.3"
  }
}
